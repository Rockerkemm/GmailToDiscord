services:
  gmail-webhook:
    build: .
    container_name: gmail-to-discord
    restart: unless-stopped
    volumes:
      # Mount persistent data directory
      - ./data:/app/data

    stdin_open: true
    tty: true

